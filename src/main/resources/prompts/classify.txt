You classify a knowledge source into exactly one of these types:

{{knownTypes}}

Definitions:
- `paper` — formal academic/research paper: abstract, methods, results, references. Peer-reviewed or preprint.
- `paper_simple` — explainer / tutorial / lighter technical writeup on a paper-like topic. Not rigorous, no methods section.
- `article` — longform blog post, essay, magazine piece. Author-voiced, argued, cites sources informally.
- `web` — short-form web page, news item, docs page, landing page.
- `youtube` — a YouTube video transcript (timestamps, speaker turns, spoken-word register).
- `podcast` — a podcast / interview transcript with multiple speakers.
- `book_chapter` — a chapter or excerpt from a book: chapter heading, sustained narrative, author POV.
- `concept_note` — the user's own note or draft — first-person, unpolished, may be outline form.
- `generic` — fallback when none of the above fit confidently.

Decision rules (apply in order):
1. If the text begins with timestamped lines (e.g. `00:01:23`) and has identifiable speakers, return `podcast` (multi-speaker) or `youtube` (single speaker / auto-captions).
2. If the text contains an `Abstract`, `References`, or numbered citation markers and is academic in register, return `paper` (rigorous) or `paper_simple` (explainer).
3. If the source path contains `raw/articles/` and the text is longform, return `article`; if shorter / URL-like, return `web`.
4. If the source path ends in `.md` and the content is first-person and unpolished, return `concept_note`.
5. If unsure, return `generic`.

Source metadata:
- Title: {{title}}
- Source path: {{sourcePath}}

First ~1500 characters of content:

```
{{preview}}
```

Return ONLY a single-line JSON object, no prose, no code fences:

{"type": "<one-of-the-types>", "confidence": "high|medium|low"}
